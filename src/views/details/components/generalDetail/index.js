export default function GeneralDetail( { fine } ) {
    return (
        <div>
            <h2>Infracción general #{fine.id}</h2>
            <p>
                <b></b>
            </p>
        </div>
    );
}